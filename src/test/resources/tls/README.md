# TLS test certificate

`localhost-cert.pem` and `localhost-key.pem` are a public, self-signed
certificate and private key used only by Cardigan's localhost tests. The key
is intentionally committed, provides no security, and must not be used outside
the test suite.
